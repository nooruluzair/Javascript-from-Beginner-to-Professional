// For reference, 1 mile equals 1.60934 kilometers

let ValueInMiles = window.prompt("Please enter value in miles >>");
let ValueInKilometers = (ValueInMiles * 1.60934);
ValueInMiles = Number(ValueInMiles);
ValueInKilometers = Number(ValueInKilometers);
alert(ValueInKilometers);
